import app from './hauler_setup_page';
import { ensurePilotSchema } from './pilot_foundation';
function json(d:any,s=200){return new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})}
function cv(r:Request,n:string){for(const p of (r.headers.get('Cookie')||'').split(';')){const [k,...v]=p.trim().split('=');if(k===n)return decodeURIComponent(v.join('='))}return null}
async function sha(v:string){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
async function customer(r:Request,e:any){const t=cv(r,'woc_session');if(!t)return null;return e.DB.prepare(`SELECT u.id,u.email FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token_hash=? AND datetime(s.expires_at)>datetime('now') LIMIT 1`).bind(await sha(t)).first<any>()}
async function api(req:Request,env:any){const c=await customer(req,env);if(!c)return json({error:'Please sign in again.'},401);await ensurePilotSchema(env);if(req.method==='GET'){const rows=await env.DB.prepare(`SELECT hp.user_id AS hauler_id,hp.business_name,COALESCE(p.preference,'allowed') AS preference FROM hauler_profiles hp LEFT JOIN customer_hauler_preferences p ON p.hauler_id=hp.user_id AND p.customer_id=? WHERE hp.status='approved' ORDER BY hp.business_name`).bind(c.id).all();return json({ok:true,haulers:rows.results||[]})}let b:any={};try{b=await req.json()}catch{};if(b.select_all===true){await env.DB.prepare(`DELETE FROM customer_hauler_preferences WHERE customer_id=? AND preference<>'excluded'`).bind(c.id).run();return json({ok:true})}const id=String(b.hauler_id||''),pref=String(b.preference||'allowed');if(!id||!['preferred','allowed','excluded'].includes(pref))return json({error:'Invalid hauler preference.'},400);if(pref==='preferred')await env.DB.prepare(`UPDATE customer_hauler_preferences SET preference='allowed',updated_at=datetime('now') WHERE customer_id=? AND preference='preferred'`).bind(c.id).run();await env.DB.prepare(`INSERT INTO customer_hauler_preferences(customer_id,hauler_id,preference) VALUES(?,?,?) ON CONFLICT(customer_id,hauler_id) DO UPDATE SET preference=excluded.preference,updated_at=datetime('now')`).bind(c.id,id,pref).run();return json({ok:true})}
function page(){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Choose Haulers | Water OnCall</title><style>*{box-sizing:border-box}body{margin:0;background:#f5faff;color:#13283d;font-family:Inter,system-ui,sans-serif}main{max-width:760px;margin:auto;padding:38px 20px}h1{color:#06325e}.card{background:#fff;border:1px solid #dce8f3;border-radius:16px;padding:18px;margin:12px 0}.row{display:flex;justify-content:space-between;gap:16px;align-items:center}.muted{color:#61778d;font-size:13px}.buttons{display:flex;gap:7px;flex-wrap:wrap}button{border:1px solid #bdd0e1;background:#fff;border-radius:9px;padding:8px 11px;font-weight:750;cursor:pointer}button.primary{background:#0877f9;color:#fff;border-color:#0877f9}.top{width:100%;height:44px;background:#0877f9;color:#fff;border:0;margin:12px 0}a{color:#0877f9;font-weight:800}</style></head><body><main><a href="/account">← Back to Customer Portal</a><h1>Choose your water haulers</h1><p class="muted">You can choose one preferred hauler. They receive the request first and have up to 24 hours to respond. Your other allowed haulers become backups. You can also exclude a company from receiving your requests.</p><button class="top" onclick="allHaulers()">Select all approved haulers as backups</button><div id="list"></div></main><script>async function load(){const r=await fetch('/api/customer/hauler-preferences'),d=await r.json();if(!r.ok){location='/login?next=/customer/haulers';return}document.getElementById('list').innerHTML=(d.haulers||[]).map(h=>'<div class="card"><div class="row"><div><strong>'+h.business_name+'</strong><div class="muted">Current: '+h.preference+'</div></div><div class="buttons"><button class="'+(h.preference==='preferred'?'primary':'')+'" onclick="setp(\''+h.hauler_id+'\',\'preferred\')">Preferred</button><button class="'+(h.preference==='allowed'?'primary':'')+'" onclick="setp(\''+h.hauler_id+'\',\'allowed\')">Backup</button><button class="'+(h.preference==='excluded'?'primary':'')+'" onclick="setp(\''+h.hauler_id+'\',\'excluded\')">Exclude</button></div></div></div>').join('')||'<div class="card muted">No approved water haulers are available yet.</div>'}async function setp(id,preference){await fetch('/api/customer/hauler-preferences',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({hauler_id:id,preference})});load()}async function allHaulers(){await fetch('/api/customer/hauler-preferences',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({select_all:true})});load()}load()</script></body></html>`,{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store','X-Frame-Options':'DENY'}})}
export default{async fetch(req:Request,env:any,ctx:ExecutionContext){const u=new URL(req.url);if(u.pathname==='/api/customer/hauler-preferences'&&(req.method==='GET'||req.method==='POST'))return api(req,env);if(u.pathname==='/customer/haulers'&&req.method==='GET')return page();return app.fetch(req,env,ctx)}};
