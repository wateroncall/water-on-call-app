import app from './customer_haulers';
function add(html:string,path:string,label:string,needle:string){if(html.includes(`href="${path}"`))return html;const link=`<a href="${path}" style="display:inline-flex;align-items:center;justify-content:center;text-decoration:none;border:1px solid #bdd0e1;border-radius:10px;padding:10px 14px;margin:6px;color:#06325e;background:#fff;font-weight:800">${label}</a>`;if(html.includes(needle))return html.replace(needle,link+needle);return html.replace('</body>',`<div style="position:fixed;right:14px;bottom:14px;z-index:9999">${link}</div></body>`)}
async function enhance(r:Response,path:string){if(!r.headers.get('Content-Type')?.includes('text/html'))return r;let h=await r.text();if(path==='/account'){h=add(h,'/customer/haulers','Choose Water Haulers','</body>')}if(path==='/hauler'){h=add(h,'/hauler/setup','Delivery Setup & Pricing','</body>')}if(path==='/admin'){h=add(h,'/admin/pilot-controls','Pilot Controls','</body>');h=add(h,'/admin/notifications','Notification History','</body>')}const headers=new Headers(r.headers);headers.delete('Content-Length');headers.set('Cache-Control','no-store');return new Response(h,{status:r.status,statusText:r.statusText,headers})}
export default{async fetch(req:Request,env:any,ctx:ExecutionContext){const u=new URL(req.url);const r=await app.fetch(req,env,ctx);if(req.method==='GET'&&['/account','/hauler','/admin'].includes(u.pathname))return enhance(r,u.pathname);return r}};
