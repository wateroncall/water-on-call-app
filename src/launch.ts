import app from "./customer_maps";

const H={"Content-Type":"text/html; charset=utf-8","Cache-Control":"no-store","X-Content-Type-Options":"nosniff","X-Frame-Options":"DENY"};
const ADMIN="admin@wateroncall.ca";
function esc(v:any){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
function json(d:any,s=200){return new Response(JSON.stringify(d),{status:s,headers:{"Content-Type":"application/json","Cache-Control":"no-store"}})}
function cookie(req:Request,n:string){for(const p of (req.headers.get('Cookie')||'').split(';')){const [k,...v]=p.trim().split('=');if(k===n)return decodeURIComponent(v.join('='))}return null}
async function sha(v:string){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
async function user(req:Request,env:any){const t=cookie(req,'woc_session');if(!t)return null;return env.DB.prepare(`SELECT users.id,users.email,users.role FROM sessions JOIN users ON users.id=sessions.user_id WHERE sessions.token_hash=? AND datetime(sessions.expires_at)>datetime('now') LIMIT 1`).bind(await sha(t)).first()}
async function schema(env:any){await env.DB.prepare(`CREATE TABLE IF NOT EXISTS launch_waitlist(id TEXT PRIMARY KEY,full_name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT,postal_code TEXT,water_need TEXT,current_hauler INTEGER NOT NULL DEFAULT 0,marketing_email INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT(datetime('now')),UNIQUE(email))`).run();await env.DB.prepare(`CREATE TABLE IF NOT EXISTS launch_settings(id INTEGER PRIMARY KEY CHECK(id=1),ordering_live INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL DEFAULT(datetime('now')) )`).run();await env.DB.prepare(`INSERT OR IGNORE INTO launch_settings(id,ordering_live) VALUES(1,0)`).run()}
async function live(env:any){await schema(env);const r=await env.DB.prepare('SELECT ordering_live FROM launch_settings WHERE id=1').first<any>();return !!r?.ordering_live}
function page(){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Water OnCall | Bulk Water Delivery</title><meta name="description" content="Water OnCall connects customers with approved bulk water haulers. Launching in Niagara, Ontario."><style>*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,sans-serif;color:#13283d;background:#f5faff}header{padding:20px max(20px,calc((100vw - 1100px)/2));background:#fff;border-bottom:1px solid #dce8f3}.brand{font-size:24px;font-weight:900;color:#06325e}.brand span{color:#0877f9}main{max-width:1100px;margin:auto;padding:60px 20px}.hero{text-align:center;max-width:800px;margin:auto}.pill{display:inline-block;background:#eaf4ff;color:#0877f9;padding:7px 12px;border-radius:999px;font-weight:800}h1{font-size:clamp(42px,7vw,72px);line-height:1.02;color:#06325e;margin:18px 0}p{color:#61778d;line-height:1.6}.grid{display:grid;grid-template-columns:1fr 1fr;gap:22px;margin-top:45px}.card{background:#fff;border:1px solid #dce8f3;border-radius:20px;padding:26px;box-shadow:0 12px 35px #06325e0d}input,select{width:100%;height:48px;border:1px solid #bdd0e1;border-radius:10px;padding:0 12px;font:inherit;margin:5px 0 12px}label{font-weight:800;font-size:13px}button,.btn{display:inline-block;border:0;border-radius:11px;background:#0877f9;color:#fff;padding:13px 17px;font-weight:850;text-decoration:none;cursor:pointer}.check{display:flex;gap:9px;align-items:flex-start;margin:10px 0 16px}.check input{width:auto;height:auto;margin-top:3px}.success{background:#e4f8ef;color:#08764b;padding:12px;border-radius:10px;margin-bottom:12px}.how{margin-top:50px;text-align:center}.steps{display:grid;grid-template-columns:repeat(3,1fr);gap:15px;margin-top:22px}.step{background:#fff;border:1px solid #dce8f3;border-radius:16px;padding:20px}.num{font-size:28px;font-weight:900;color:#0877f9}@media(max-width:750px){.grid,.steps{grid-template-columns:1fr}}</style></head><body><header><div class="brand"><span>Water</span> OnCall</div></header><main><section class="hero"><span class="pill">Launching soon in Niagara</span><h1>Bulk water delivery. A simpler way.</h1><p>Water OnCall connects customers who need bulk water with an approved network of local water haulers. Join the launch list or apply to become a Water OnCall hauler.</p></section><div class="grid"><section class="card"><h2>Need bulk water?</h2><p>Join our customer launch list and we'll let you know when ordering opens in your area.</p><div id="msg" hidden class="success"></div><form id="wait"><label>Name</label><input name="full_name" required maxlength="100"><label>Email</label><input name="email" type="email" required maxlength="254"><label>Mobile phone <span style="font-weight:400">(optional)</span></label><input name="phone" type="tel" placeholder="905-555-1234"><label>Postal code / community</label><input name="postal_code" maxlength="30" placeholder="L0R 1B0 or Fonthill"><label>What do you usually need water for?</label><select name="water_need"><option value="cistern">Cistern</option><option value="pool">Pool</option><option value="hot_tub">Hot tub</option><option value="other">Other</option></select><label class="check"><input type="checkbox" name="current_hauler"><span>I currently use a water hauler</span></label><label class="check"><input type="checkbox" name="marketing_email" required><span>Yes, email me about Water OnCall launch availability and service updates.</span></label><button>Join the launch list</button></form></section><section class="card"><h2>Are you a water hauler?</h2><p>We're building an approved network of bulk water haulers. Apply now to be considered for the Water OnCall network before customer ordering opens.</p><a class="btn" href="/login?next=/hauler">Apply to join the network</a><p style="font-size:13px;margin-top:18px">Approved haulers control their pricing, service area, fill stations and availability.</p></section></div><section class="how"><h2>How Water OnCall will work</h2><div class="steps"><div class="step"><div class="num">1</div><strong>Request water</strong><p>Customers enter their delivery details and preferences.</p></div><div class="step"><div class="num">2</div><strong>Approved hauler accepts</strong><p>The request is matched with an approved bulk water hauler.</p></div><div class="step"><div class="num">3</div><strong>Water delivered</strong><p>Customers track the delivery through their Water OnCall account.</p></div></div></section></main><script>document.querySelector('#wait').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,b=Object.fromEntries(new FormData(f));b.current_hauler=!!b.current_hauler;b.marketing_email=!!b.marketing_email;const r=await fetch('/api/launch/waitlist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const d=await r.json();if(!r.ok)return alert(d.error);const m=document.querySelector('#msg');m.hidden=false;m.textContent='Thanks — you’re on the Water OnCall launch list.';f.reset()}</script></body></html>`,{headers:H})}
async function signup(req:Request,env:any){await schema(env);let b:any={};try{b=await req.json()}catch{}const name=String(b.full_name||'').trim(),email=String(b.email||'').trim().toLowerCase(),phone=String(b.phone||'').trim(),postal=String(b.postal_code||'').trim(),need=['cistern','pool','hot_tub','other'].includes(b.water_need)?b.water_need:'cistern';if(!name)return json({error:'Enter your name.'},400);if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email))return json({error:'Enter a valid email address.'},400);if(!b.marketing_email)return json({error:'Please confirm that we may email you about the Water OnCall launch.'},400);if(phone){let d=phone.replace(/\D/g,'');if(d.length===11&&d[0]==='1')d=d.slice(1);if(d.length!==10)return json({error:'Enter a valid 10-digit phone number or leave it blank.'},400)}await env.DB.prepare(`INSERT INTO launch_waitlist(id,full_name,email,phone,postal_code,water_need,current_hauler,marketing_email) VALUES(?,?,?,?,?,?,?,1) ON CONFLICT(email) DO UPDATE SET full_name=excluded.full_name,phone=excluded.phone,postal_code=excluded.postal_code,water_need=excluded.water_need,current_hauler=excluded.current_hauler,marketing_email=1`).bind(crypto.randomUUID(),name,email,phone||null,postal||null,need,b.current_hauler?1:0).run();return json({ok:true})}
async function adminPage(req:Request,env:any){const u:any=await user(req,env);if(!u||u.email!==ADMIN)return Response.redirect(new URL('/login',req.url).toString(),302);await schema(env);const s=await env.DB.prepare('SELECT ordering_live FROM launch_settings WHERE id=1').first<any>();const c=await env.DB.prepare('SELECT COUNT(*) count FROM launch_waitlist').first<any>();return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Launch Mode</title><style>body{font-family:system-ui;background:#f5faff;color:#13283d;max-width:760px;margin:auto;padding:40px 20px}.card{background:#fff;border:1px solid #dce8f3;border-radius:18px;padding:24px;margin-bottom:18px}button{background:#0877f9;color:#fff;border:0;border-radius:10px;padding:12px 16px;font-weight:800}</style></head><body><h1>Water OnCall Launch Mode</h1><div class="card"><h2>Customer ordering</h2><p>Current mode: <strong>${s?.ordering_live?'LIVE':'TESTING / COMING SOON'}</strong></p><button id="toggle">Switch to ${s?.ordering_live?'Coming Soon':'Live'}</button></div><div class="card"><h2>Launch list</h2><p><strong>${Number(c?.count||0)}</strong> interested customers collected.</p></div><script>document.querySelector('#toggle').onclick=async()=>{const r=await fetch('/api/admin/launch-mode',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ordering_live:${s?.ordering_live?'false':'true'}})});if(r.ok)location.reload()}</script></body></html>`,{headers:H})}
export default{async fetch(req:Request,env:any,ctx:ExecutionContext){const url=new URL(req.url);if(url.pathname==='/api/launch/waitlist'&&req.method==='POST')return signup(req,env);if(url.pathname==='/admin/launch'&&req.method==='GET')return adminPage(req,env);if(url.pathname==='/api/admin/launch-mode'&&req.method==='POST'){const u:any=await user(req,env);if(!u||u.email!==ADMIN)return json({error:'Admin required'},403);await schema(env);let b:any={};try{b=await req.json()}catch{}await env.DB.prepare(`UPDATE launch_settings SET ordering_live=?,updated_at=datetime('now') WHERE id=1`).bind(b.ordering_live?1:0).run();return json({ok:true})}if(url.pathname==='/'&&req.method==='GET'&&!(await live(env)))return page();if(url.pathname==='/account'&&req.method==='GET'&&!(await live(env))){const u:any=await user(req,env);if(!u)return Response.redirect(new URL('/',req.url).toString(),302);/* signed-in test accounts remain able to test */}return app.fetch(req,env,ctx)}};
